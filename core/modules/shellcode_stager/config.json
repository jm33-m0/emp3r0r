{
  "name": "shellcode_stager",
  "build": "make LISTENER_TYPE=$LISTENER_TYPE",
  "author": "jm33-ng",
  "date": "2025-12-12",
  "comment": "A Linux shellcode stager that supports payload delivery via HTTP/TCP/UDP with encryption and compression. Use with emp3r0r-listener",
  "is_local": true,
  "platform": "Linux",
  "path": "",
  "parameters": [
    {
      "name": "LISTENER_TYPE",
      "description": "Listener protocol type: HTTP, TCP, or UDP",
      "default": "HTTP",
      "choices": [
        "HTTP",
        "TCP",
        "UDP"
      ],
      "type": "enum"
    },
    {
      "name": "DOWNLOAD_HOST",
      "description": "The host to download the payload from, eg. 192.168.0.1",
      "default": "",
      "type": "string"
    },
    {
      "name": "DOWNLOAD_PORT",
      "description": "The port to download the payload from, eg. 8000",
      "default": "8000",
      "type": "uint"
    },
    {
      "name": "DOWNLOAD_PATH",
      "description": "The path to download the payload from (HTTP only), eg. /payload",
      "default": "/",
      "type": "string"
    },
    {
      "name": "DOWNLOAD_KEY",
      "description": "The key to decrypt the payload",
      "default": "",
      "type": "string",
      "secret": true
    }
  ],
  "agent_config": {
    "exec": "",
    "files": [],
    "in_memory": false,
    "type": "",
    "interactive": false
  },
  "invocation": {
    "argv": []
  }
}