{
  "name": "stager",
  "build": "if [ \"$BUILD_EXE\" = \"yes\" ]; then make stager_exe LISTENER_TYPE=$LISTENER_TYPE; else make stager_so LISTENER_TYPE=$LISTENER_TYPE; fi",
  "author": "jm33-ng",
  "date": "2025-12-12",
  "comment": "A Linux stager that supports payload delivery via HTTP/TCP/UDP with encryption and compression. Use with emp3r0r-listener",
  "is_local": true,
  "platform": "Linux",
  "path": "",
  "parameters": [
    {
      "name": "BUILD_EXE",
      "description": "Build stager as an executable, otherwise a shared object will be built",
      "default": "no",
      "choices": [
        "yes",
        "no"
      ],
      "type": "enum"
    },
    {
      "name": "LISTENER_TYPE",
      "description": "Listener protocol type: HTTP, TCP, or UDP",
      "default": "HTTP",
      "choices": [
        "HTTP",
        "TCP",
        "UDP"
      ],
      "type": "enum"
    },
    {
      "name": "ARCH",
      "description": "The architecture of the target system, use GOARCH values",
      "default": "amd64",
      "choices": [
        "amd64",
        "386",
        "arm",
        "arm64",
        "riscv64",
        "mips",
        "mips64",
        "ppc64"
      ],
      "type": "enum"
    },
    {
      "name": "DOWNLOAD_HOST",
      "description": "The host to download the payload from, eg. 192.168.0.1",
      "default": "",
      "type": "string"
    },
    {
      "name": "DOWNLOAD_PORT",
      "description": "The port to download the payload from, eg. 8000",
      "default": "8000",
      "type": "uint"
    },
    {
      "name": "DOWNLOAD_PATH",
      "description": "The path to download the payload from (HTTP only), eg. /payload",
      "default": "/",
      "type": "string"
    },
    {
      "name": "DOWNLOAD_KEY",
      "description": "The key to decrypt the payload",
      "default": "",
      "type": "string",
      "secret": true
    }
  ],
  "agent_config": {
    "exec": "",
    "files": [],
    "in_memory": false,
    "type": "",
    "interactive": false
  },
  "invocation": {
    "argv": []
  }
}