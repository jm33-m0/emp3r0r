[
  {
    "name": "svc_loader",
    "build": "bash ./build.sh",
    "author": "jm33-ng",
    "date": "2026-09-08",
    "comment": "Generate a one-shot Windows service executable that early-bird injects RC4-encrypted Donut sRDI shellcode (e.g. the agent.exe.bin produced by `agent generate --type exe`) into a sacrificial process (svchost.exe, dllhost.exe). The service starts, injects, verifies the child, then stops itself.",
    "is_local": true,
    "platform": "Windows",
    "path": "",
    "fileless": true,
    "parameters": [
      {
        "name": "shellcode",
        "description": "Path to the Donut sRDI shellcode (.bin) on the C2, e.g. the agent.exe.bin generated by `agent generate`",
        "default": "",
        "type": "string",
        "required": true
      },
      {
        "name": "output",
        "description": "Output .exe path (default: <shellcode dir>/<name>_svc.exe)",
        "default": "",
        "type": "string",
        "required": false
      },
      {
        "name": "process",
        "description": "Sacrificial process to spawn and inject into: bare name resolved under System32 (svchost.exe, dllhost.exe, ...) or a full path",
        "default": "svchost.exe",
        "type": "string",
        "required": false
      },
      {
        "name": "process-args",
        "description": "Optional command-line arguments for the sacrificial process (the primary thread is parked before its code runs, so these rarely matter)",
        "default": "",
        "type": "string",
        "required": false
      },
      {
        "name": "arch",
        "description": "Architecture of the shellcode and of the generated loader; must match the Donut blob",
        "default": "x64",
        "choices": ["x64", "x86"],
        "type": "string",
        "required": false
      },
      {
        "name": "key",
        "description": "RC4 key as hex (1..256 bytes); leave empty to generate a fresh random key per build",
        "default": "",
        "type": "string",
        "required": false,
        "secret": true
      },
      {
        "name": "inject",
        "description": "Load method baked into the loader: apc = early-bird QueueUserAPC into a parked sacrificial (default), ct = classic CreateRemoteThread (sacrificial must stay alive, e.g. notepad.exe or a real service host)",
        "default": "apc",
        "choices": ["apc", "ct"],
        "type": "string",
        "required": false
      },
      {
        "name": "debug",
        "description": "Keep verbose diagnostics in the binary (default false: production builds compile all logging out)",
        "default": "false",
        "choices": ["false", "true"],
        "type": "string",
        "required": false
      }
    ]
  }
]
